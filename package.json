{
  "name": "autozap",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc -p tsconfig.json && cp -R src/wapi dist"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "playwright": "^1.26.1",
    "@wppconnect/wa-js": "^2.13.2"
  },
  "devDependencies": {
    "@types/node": "^18.8.3",
    "typescript": "^4.8.4",
    "ts-node": "^10.9.1",
    "rimraf": "^3.0.2"
  }
}
